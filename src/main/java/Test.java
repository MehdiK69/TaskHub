import enums.Priorite;
import enums.Statut;
import models.*;
import services.*;

import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

public class Test {
    public static void main(String[] args){


       /* Tache t1 = new Tache(1,"appli java","appli TaskHub a coder", Priorite.MOYENNE, Statut.A_FAIRE, LocalDate.now(),LocalDate.now().plusMonths(2),1,"Projet",false);
        Tache t2 = new Tache(2,"saé 2nd semestre","appli machin learning", Priorite.HAUTE, Statut.A_FAIRE, LocalDate.now(),LocalDate.now().plusMonths(3),2,"Projet",false);
        Tache t3 = new Tache(3,"Repondre email ecole","repondre pour l'inscription", Priorite.BASSE, Statut.EN_COURS, LocalDate.now(),LocalDate.now().plusDays(7), 3,"Mail",false);
        Tache t4 = new Tache(4,"rdv orthodontiste","rdv orthondiste isma", Priorite.HAUTE, Statut.TERMINEE, LocalDate.now(),LocalDate.now(),4,"Medical",true);

        TacheService service = new TacheService();

        // Ajouter 5 tâches
        service.ajouterTache(t1);
        service.ajouterTache(t2);
        service.ajouterTache(t3);
        service.ajouterTache(t4);


        // Afficher
        service.afficherTaches();

        // Marquer 2 comme terminées
        service.marquerTerminee(3);

        // Afficher
        service.afficherTaches();

        // Supprimer 1 tâche
        service.supprimerTache(t4);

        // Afficher
        service.afficherTaches();

        // Stats
        System.out.println("Total : " + service.getNombreTaches());
        System.out.println("Terminées : " + service.getNombreTachesTerminees());*/
    }

}
